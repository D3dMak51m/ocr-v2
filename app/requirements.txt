pytesseract==0.3.13
requests==2.32.3
uvicorn==0.30.6
fastapi==0.95.0
numpy==1.24.1
pandas==2.0.0
PyMuPDF==1.24.10
beautifulsoup4==4.12.3
chardet==5.2.0
python-dotenv==1.1.1
opencv-python-headless==4.8.1.78

# Excel processing with encoding support
openpyxl==3.1.2    # For reading/writing XLSX files with Unicode support
xlrd==2.0.1        # For reading XLS files (older Excel format) with encoding support

# Optional but recommended for text encoding issues
ftfy==6.1.1        # Fixes text with mojibake and other encoding problems
uvloop==0.19.0  # Faster event loop for uvicorn (10-30% speed improvement)